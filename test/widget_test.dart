import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (WidgetTester tester) async {
    // This test is currently disabled/placeholder as we rely on integration tests.
    expect(true, isTrue);
  });
}
