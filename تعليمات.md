# سحب التحديثات

git pull origin main

# تثبيت المكاتب (للتأكد فقط)

flutter pub get

# ⚠️ هام: لتشغيل التطبيق الآن يجب تحديد النسخة (dev أو prod)

# لتشغيل نسخة المطورين (للتجارب)

flutter run --flavor dev -t lib/main_dev.dart

# لبناء ملف APK لنسخة المطورين

flutter build apk --flavor dev -t lib/main_dev.dart
